import SelectDiscordBot from "@/app/clone/[id]/SelectDiscordBot";

function Page(props: any) {
	return (
		<div>
			<SelectDiscordBot title={'Select Copier Bot'} description={'Which is act like an bot to create channel/role from source server'} />
		</div>
	);
}

export default Page;
